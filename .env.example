# Required: path to SQLite database file
DATABASE_URL="file:./prisma/dev.db"

# Required: strong secret for JWT signing (change this!)
JWT_SECRET="change-me-to-a-random-32-character-string"

# Optional: server port (default 3000)
PORT=3000

# Optional: set to "production" when running built app
NODE_ENV="development"
